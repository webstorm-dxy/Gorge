# Gorge 项目规则

## 语言要求
- 所有回复默认使用中文
- 代码中的变量、函数、类型名称应有实际含意

## 工作流程
1. **方案先于执行** — 用户未明确要求执行时，只制定方案，不执行
2. **不确定必问** — 任何不确定的地方（技术选型、实现方式、用户意图），必须先询问用户，不可猜测；觉得自己没理解用户意图时，应请用户提供更多信息
3. **方案确认** — 用户提出的方案可能不是最优，应主动与用户讨论并确定最终方案，待用户确认后再执行
4. **了解用户修改** — 每次对话开始或修改文件前，应确认用户手动修改了哪些地方；修改用户编写的代码前需向用户确认
5. **先查知识库** — 每次新对话开始时，应先从 `references/` 文件夹中读取对应的参考资料，仔细研究后再开展工作

## 代码质量标准
- **零 Warning** — 编译不允许有任何 warning，如有必须修复或与用户确认处理方式
- **自动化测试** — 编写合理的单元测试，确保功能正确
- **代码整洁** — 变量/函数/类型命名有实际含意，代码结构清晰
- **SOLID 原则** — 合理应用设计模式，符合软件工程规范
- **完整性** — 保证代码功能完整，不遗漏边界情况
- **中文注释** — 注释使用符合 Rust 文档规范的中文，`///` 用于公共 API 文档，`//` 用于内部逻辑说明；注释需要解释清楚功能、参数、返回值和注意事项

## 项目维护
- 多次修改产生的无关文件和代码，需经用户确认后清理
- 用户可能手动修改代码，每次修改前应确认用户改了什么
- **时刻更新 AGENTS.md** — 规则有变化时应立即同步到 AGENTS.md
- **Git 操作** — 用户要求编写提交信息时，只输出提交信息内容，不执行 git 操作

## 记忆管理
- **MEMORY.md** — 每次回复后应将记忆内容写入 `MEMORY.md`，每个会话开始时必须先读取 `MEMORY.md` 了解当前项目状态和进展

## 动态规则
- 持续分析用户行为，必要时向用户确认后添加新规则
